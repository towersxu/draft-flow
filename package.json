{
  "name": "draft-flow",
  "version": "1.0.0",
  "description": "手绘风格流程图",
  "scripts": {
    "setup": "rm -rf node_modules && rm -rf ./packages/*/node_modules && pnpm i && npm run build",
    "build": "pnpm --filter=\"./packages/**\" -r --steam build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/towersxu/rough-flow"
  },
  "devDependencies": {
    "pnpm": "^8.9.0"
  },
  "author": "xutao",
  "license": "Apache-2.0"
}
